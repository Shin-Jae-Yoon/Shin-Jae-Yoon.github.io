public
node_modules
.quartz-cache
# Migrated author content is hash-verified against immutable source evidence.
content
# Run artifacts: proposals, reviews and reports the knowledge engine writes and
# reads back byte for byte. Reformatting them would invalidate the run.
.omx
# Batch fixtures are engine input, generated by the authoring lane.
tooling/brain/fixtures
